# Basic environment for deep learning. For situations where torch needs to be processed.
name: dl_env

channels:
#  - nvidia  # if install CUDA version torch related dependencies
  - pytorch  # if using conda to install pytorch related dependencies
  - conda-forge
  - nodefaults

dependencies:
  - python=3.12
# install torch related dependencies by conda
#  - pytorch  # In conda, the name is pytorch. In pip, the name is torch.
#  - torchvision
#  - torchaudio
# install other dependencies by pip
  - pip
  - pip:
# utils
    - notebook
    - ipywidgets

# main
# pytorch
    - torch
#    - torchvision
#    - torchaudio
# lightning
    - lightning
    - torchmetrics
# logger
    - wandb
    - tensorboard
# config
    - hydra-core

# nlp
#    - sentencepiece

# cv
#    - opencv-python
#    - moviepy

# audio
#    - soundfile


# data processing, analysis, modeling (better in ds_env)
    - pandas
#    - statsmodels
#    - scikit-learn  # sklearn

# path
    - pathlib
    - omegaconf

# compatibility
## json related
    - orjson
    - ujson
    - jsonlines
    - json-repair  # llm
## xlsx
    - openpyxl
#    - xlrd  # read .xls file


# test
    - pytest

# specific task pip_requirements


#    - -r ../pip_requirements/dl_env.txt
#    - -r ../pip_requirements/sub_requirements/dl_tools.txt
#  - run:
#      - ./conda_finish.sh


# conda env create --file=./conda_environments/dl_env.yaml
