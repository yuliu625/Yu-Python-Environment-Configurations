#
name: _env

channels:
  - conda-forge
  - nodefaults

dependencies:
  - python=3.13
  - pip
  - pip:
# utils
    - notebook
    - ipywidgets

# main
    -


# path
    - pathlib
    - omegaconf

# log
    - loguru

# test
    - pytest


#    - -r pip_requirements/_env.txt
#    - -r pip_requirements/another_config_file.txt
#  - run:
#      - ./conda_finish.sh


# conda env create --file=./conda_environments/$name_env.yaml
