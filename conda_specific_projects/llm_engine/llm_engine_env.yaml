# 专门用于部署llm推理服务的独立环境，
name: llm_engine_env

channels:
#  - nvidia  # if install CUDA version torch related dependencies
#  - pytorch  # if using conda to install pytorch related dependencies
  - conda-forge
  - nodefaults

dependencies:
  - python=3.12
# install torch related dependencies by conda
#  - pytorch  # In conda, the name is pytorch. In pip, the name is torch.
#  - torchvision
#  - torchaudio
# install other dependencies by pip
  - pip
  - pip:
# utils
    - notebook
    - ipywidgets
    - loguru


# main
## core frameworks / inference engines
#    - vllm
#    - sglang
#    - tensorrt-llm
#    - text-generation-inference
#    - lmdeploy
    - litserve
## gateway
    - litellm[proxy]


# path
    - pathlib
    - omegaconf


# test
    - pytest


#    - -r pip_requirements/_env.txt
#    - -r pip_requirements/another_config_file.txt
#  - run:
#      - ./conda_finish.sh

# conda env create --file=./conda_specific_projects/llm_engine/llm_engine_env.yaml

