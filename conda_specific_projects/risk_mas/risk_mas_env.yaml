#
name: risk_mas_env

channels:
#  - nvidia  # if install CUDA version torch related dependencies
  - pytorch  # if using conda to install pytorch related dependencies
  - conda-forge
  - nodefaults

dependencies:
  - python=3.12
# install torch related dependencies by conda
  - pytorch  # In conda, the name is pytorch. In pip, the name is torch.
#  - torchvision
#  - torchaudio
# install other dependencies by pip
  - pip
  - pip:
# utils
    - notebook
    - ipywidgets
    - pandas
    - pathlib

# main
# framework
## langgraph
    - langgraph
    - langchain
### langchain community
    - langchain-community  # for community LLMs, embedding models, tools
    - langchain-openai  # openai client. for ChatOpenAI
    - langchain-huggingface  # huggingface endpoint
    - langchain-ollama
#    - runhouse
#    - einops
### loader
    - langchain-pymupdf4llm==0.3.1  # 0.4.0 can't run
    - rapidocr-onnxruntime
    - pillow
### vector store
    - langchain-chroma

# test
    - pytest


#    - -r pip_requirements/_env.txt
#    - -r pip_requirements/another_config_file.txt
#  - run:
#      - ./conda_finish.sh

# conda env create --file=./conda_specific_projects/risk_mas/risk_mas_env.yaml
