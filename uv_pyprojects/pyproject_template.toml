[project]
name = "uv-project"
version = "0.1.0"
description = "UV projects configuration file template."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # ==== Base ====
    ## utils
    "notebook",
    "ipywidgets",
    ## log
    "loguru",
    ## path
    "pathlib",
    "omegaconf",
    ## tests
    "pytest",
]


[dependency-groups]
dev = [

]

[tool.uv]
#default-groups = [""]

